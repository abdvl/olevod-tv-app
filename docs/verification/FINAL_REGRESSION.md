# 最新已安装 APK 独立回归（阶段记录）

2026-09-07。仅操作主 agent 已装 APK，未构建/安装、未读账号凭据。用户验证码到达后，按主 agent 要求立即交还模拟器；以下为已完成范围，不以标题宣称整个项目最终验收。

## 已完成

- 搜索冷启动：Down聚焦输入框时 `mInputShown=false`；OK进入编辑Dialog后true；第一次Back关闭IME为false，第二次Back回搜索页。
- 从搜索输入返回后Down可到操作栏、自绘键盘；确定字母c，界面/语义树显示c。
- 清空后选择热词“早春晴朗”，真实返回1部、有标题/海报/评分/集数，见final-search-results.png。
- 点播首键修复：非全屏播放静置后第一次OK直接PAUSED，位置17206ms；不再要求第二次按键。
- 全屏播放控制隐藏后，确定可以唤醒控制条，焦点显示在播放/暂停按钮；见final-full-wakeup-playing.png。截图操作紧接按键以免5秒自动隐藏干扰观察。
- 系统Home后MediaSession为PAUSED，位置59409ms；后台返回仍保留位置。
- 重启历史页显示影片记录02:00/1:55:39，说明播放周期进度持久化。该轮force-stop前会话约126s，最后周期120s；未把强制进程终止当作可保证最后毫秒写入。已点击记录进入续播，但验证码任务抢占前尚未读取续播后的实际位置，不能称续播完整验收。

## 因交还模拟器尚未完成

- 历史点击后的实际恢复位置。
- 从列表进播放器再返回的卡片焦点/滚动恢复。
- 最新首页分类进入第二行时标题和2×5全部图文完整显示。

## 不在本轮范围

真实登录/VIP由主 agent 使用用户验证码继续验证；直播子清单失败由主 agent 记录。本轮不对这些作通过结论。此前协议13项与数据库25条测试是主 agent 已告知结果，不冒充本轮重复运行。

截图目录：`docs/verification/screenshots/final-*.png`，均无账号。

## 登录完成后的追加独立 UI 验证

继续使用主 agent 已装版本，仅操作 UI，不安装、不构建、不操作收藏。完成后已明确交还模拟器。本段覆盖上文部分待测项与直播限制。

- CCTV-13 新闻：实际新闻视频正常显示，MediaSession `PLAYING`、位置8964ms；截图 `live-cctv13-attempt.png`。
- CCTV-13 节目单《午夜新闻》：点击回看后标题明确切换为回看，实际视频与 `PLAYING`、位置16267ms；截图 `live-replay-success.png`。
- 东方卫视HD：首次显示 Source error，播放位置0；通过界面重试后实际视频正常显示，`PLAYING`、位置6971ms。截图 `live-dongfang.png` 为失败状态，`live-dongfang-retry-success.png` 为恢复状态。说明重试可恢复此次故障，不能推断所有频道源稳定。
- 一次误导航进入 CCTV-1 时也出现 Source error，留图 `live-cctv1-error.png`；未进一步验证该频道，不将其计入通过频道。
- 首页“短剧”第二行第二张聚焦时，分类标题、2×5海报、两行片名和年份/地区全部位于可见范围，无底部裁切；截图 `final-home-two-rows.png`。
- 从该位置进入《张家女麒麟》实际播放，退出后恢复同一张卡片焦点与原滚动位置；截图 `final-home-focus-return.png`。此轮退出使用了两次 Back（第一次后仍在播放器，第二次回列表），不据此宣称单次 Back 行为通过。

仍未补测历史记录点击后的精确恢复位置；登录/VIP由主 agent 独立证据覆盖，非本轮 UI 验证内容。模拟器无音频输出，未验证听感、真机4K/HDR及性能。
