# v0.3 与 v0.2 发布收尾 Token history

统计日期：2026-09-12（America/Los_Angeles）。[返回 Token history](../../README.md#token-history) · [逐请求用量元数据与检查点](v0.3-2026-09-12.json)

本次从上一统计截止点 **2026-09-08 21:18:45.833 PDT**（UTC 2026-09-09T04:18:45.833Z）之后开始，截止于本次统计请求之前 **2026-09-12 10:22:48.887 PDT**（UTC 2026-09-12T17:22:48.887Z，右端不含）。本次核算、文档更新及推送不计入。历史行保持原值。

分开记录两段工作：v0.2 最终 README、截图、安装与发布核验，以及 v0.3 设置页、更新检查、测试、签名包、推送与发布。前者沿用上一阶段主任务及子任务范围，后者统计本任务。独立的键盘使用、机器功耗等任务不计入。

| 阶段 | 请求数 | 输入（含缓存） | 缓存输入 | 输出（含推理） | 总 token | Standard API 估值（USD） |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| v0.2 最终文档、截图与发布 | 305 | 42,003,836 | 40,997,376 | 111,508 | 42,115,344 | **$56.64** |
| v0.3 设置开发、测试与发布 | 95 | 10,634,693 | 10,395,264 | 23,008 | 10,657,701 | **$13.94** |

两段工作均使用 GPT-6 Astra。v0.2 收尾最后一条用量记录为 **2026-09-09 18:21:55.908 PDT**；v0.3 最后一条为 **2026-09-11 21:54:50.626 PDT**，已包含发布完成答复。工作模型新增共 **52,773,045 token**，估值 **$70.58**。

## 核算方法

- 按拥有日志的 thread_id 筛选 `token_usage_record.usage`，按 response_id 去重，并检查跨任务 response_id 唯一；不累加重复的 token_count 界面快照，不把继承的上下文当作独立请求再加一次。
- 对全部 5 个有增量的工作任务与 1 个系统审批任务，累计逐请求账本均与末条 thread_token_usage 一致；逐请求记录含压缩请求（如有），不会漏掉仅从快照相减可能遗漏的用量。
- 缓存输入已包含在输入总数；推理输出已包含在输出总数。非缓存输入分别为 1,006,460 和 239,429；推理输出分别为 29,821 和 2,092。缓存写入均为 0。
- 单次最大输入分别为 229,961 与 162,304，均低于 272K，未触发长上下文倍率。

## 价格与未统计项目

2026-09-12 实际打开核对的 [GPT-6 Astra 官方价格](https://developers.openai.com/api/docs/models/gpt-6-astra)：Standard 每百万 token 非缓存输入 $10、缓存输入 $1、输出 $50；缓存写入 $12.50，本区间记录为 0。

计算式：`非缓存输入 × 10 / 1e6 + 缓存输入 × 1 / 1e6 + 输出 × 50 / 1e6`。未舍入费用分别为 $56.637376、$13.939954，先相加再保留两位小数。本表为 Standard 等价估值，不代表 Codex 订阅费用、实际服务档位或账单。

平台自动审批另有 **17 次请求、656,717 token**；缺少可核对公开单价，费用未统计，单列且不混入工作模型合计。图片生成、工具费用及税费也未计入，不按零处理。只保存时间、任务、模型、请求标识和用量元数据，不复制对话、凭据或原始日志。
